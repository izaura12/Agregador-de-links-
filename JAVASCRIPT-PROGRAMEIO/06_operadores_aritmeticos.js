// PRINCIPAIS OPERADORES

var primeiroNumero = 5;
var segundoNumero = 20;

  //console.log(primeiroNumero + segundoNumero), ("soma");
  //console.log(segundoNumero - primeiroNumero);
  //console.log(primeiroNumero * segundoNumero );
  //console.log(segundoNumero / primeiroNumero);
  //console.log(segundoNumero % primeiroNumero);

// EXPRESSÕES NÚMERICAS

// se colocar os () essa parte é resolvida primeiro. os parenteses nesse caso 
// se chama escopo ele é resolvido primeiro naordem de resolução
// pode ser usado dois nesse caso ele soma os 10 + 4 depois subtrai dos 15 e depois resolve o restante.

var  expressoes = 2*50/((4 + 10) - 15);

console.log(expressoes);